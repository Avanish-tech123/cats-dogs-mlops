apiVersion: v1
kind: Service
metadata:
  name: cat-dog-classifier
spec:
  selector:
    app: cat-dog-classifier
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
  type: NodePort
